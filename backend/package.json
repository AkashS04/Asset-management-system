{
  "name": "mock-backend",
  "version": "1.0.0",
  "scripts": {
    "server":"json-server --watch db.json --port 5000",
    "start": "json-server --watch db.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}